services:
  - type: web
    name: financial-analytics-ai
    env: python
    buildCommand: ./build.sh
    startCommand: cd backend_fsa && uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
